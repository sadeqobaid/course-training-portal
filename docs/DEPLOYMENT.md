# Local deployment

Start database and local email capture with Docker Compose. Start the API, browser frontend, and reminder worker in three separate terminal windows. Use the health endpoint before opening the browser page.
